@model IEnumerable<ContosoUniversity.Models.Enrollment>


@if (Model != null)
{
    <h3>
        Sinh viên ghi danh vào khóa học đã chọn
    </h3>
    <table class="table">
        <tr>
            <th>Tên</th>
            <th>Điểm</th>
        </tr>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @item.Student.FullName
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Grade)
                </td>
            </tr>
        }
    </table>
}